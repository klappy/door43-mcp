<!-- Kirigami:unfold of docs/LENSES.tsv — regenerated, never hand-edited (cookbook/lenses/RECEIPT.md).
binding.sha: f833d6545c750402
inputs: sourceSha 15d375d · lensVersion frame-1.0.0/bodies-0.1.0 · foldoutSha 71027d52d8c687f7 · filters {"filters":{},"resolution":"long","tiers":[1,2,3]} · schema v1.0.1
stale: false (at generation, 2026-09-04T00:4xZ)
Unfold call: {tier:[1,2,3], resolution:"long", sourceSha:"15d375d", lensVersion:"frame-1.0.0/bodies-0.1.0"} -->
# Unfold — 27 row(s) · resolution long · tier 1/2/3

_Projection of the foldout (sha-bound, regenerable) — not a stored artifact._

- **lens:driver-seat** · `d0001` · trace ·t2
  Shaping lens already ran on this plan; receipt is docs/DELTA.md (pass 2026-09-03T03:53Z, re-checked 20:16Z)
  > Not re-run this dish: the driver's seat edits docs and this dish is the evaluating phase (ticket L15). DELTA honored per LIFECYCLE 1.2.0 standing bind for tickets ordered before 2026-09-03.
- **lens:klappy-architect** · `d0002` · trace ·t3
  oddkit_resolve klappy://canon/lenses/klappy-architect → NOT_FOUND 00:31Z; body read from klappy.dev cook/lf-portable-lenses bf651f2 (klappy.dev#321, draft)
  > INDEX-TEMPLATE: resolution failure is a trace row, not a skip. Ran the branch body at body 0.1.0; every row below stays custody=run until the captain acks the lens itself.
- **lens:klappy-architect** · `d0003` · crystallize:accept ·t2
  Writes ride the DO-held grant; no seat holds a secret to mutate; refused[] is a server floor
  > Architect q1 satisfied by construction. The one credential a seat cannot mint (T12 AE read token) is HUMAN-ONLY(secret) and named.
- **lens:klappy-architect** · `d0004` · crystallize:accept ·t1
  AGENTS.md L7 and CHARTER §What say "Three tools"; v2.8 ships a fourth under dcs:write with no done-means to recut them
  > A cold seat reads AGENTS.md before docs(); when v2.8 lands it boards on a false count. PLAN v2.8 done-means gains: AGENTS.md identity line and CHARTER §What recut (CHARTER is captain voice → named, not edited).
- **lens:klappy-architect** · `d0005` · crystallize:accept ·t2
  BORROW-EVALUATION covers v1 (2026-09-02); v2.5 runner, v2.6 hand-off, v2.8 seal have no 6B row
  > FIRE-CHECK gate 7 will demand it at each fire; PLAN says so now so the cook does not discover it at the stove. Candidates to name then: MCP tasks/async patterns (P0005), cartographer recipe prose, workers-oauth-provider scopes.
- **lens:klappy-architect** · `d0006` · uncertain-encode ·t2
  The tally has one home (D1 exact); T12 frames the second channel as a credential gap, the architect frames it as a single point
  > Defer: D1 is durable and the tally is derivative of upstream truth, not testimony. Re-ask when a consumer bills from it (v2.7 my-spend) — then a second home is owed.
- **lens:klappy-architect** · `d0007` · crystallize:accept ·t2
  House words (envelope, pre-formed call, pin, tally) each carry a neutral lineage name in DELTA; L1 items must cite those, not the house words
  > Already answered in DELTA's prior-art block; the finding is that PLAN v2.x done-means does not say the lineage must ride into the L1 URIs. One clause added.
- **lens:klappy-architect** · `d0008` · crystallize:accept ·t2
  v2.6 hand-off is a pre-formed call the agent executes; the server dispatches, never runs the sibling
  > Dispatcher/executor split holds. Owner column already names cartographer's owner as consulted.
- **lens:klappy-architect** · `d0009` · trace ·t2
  Every v2 row's done-means is readable off git or the live server by a stranger; v2.x depends on the T4 resolver that 404s on case
  > Nothing to move. v2.x done-means already requires resolution; T4 is the risk it names.
- **lens:klappy-architect** · `d0010` · crystallize:accept ·t2
  Failure modes and required responses live in DELTA §Retraction, not beside the PLAN rows they govern
  > Nothing missing in substance; PLAN v2.8 row gains one pointer to the v2.8 retraction condition so the cook reads it at fire.
- **lens:persona-agent-reader** · `d0011` · crystallize:accept ·t2
  docs() at 1215 B names journeys and cites both URIs; the reader orients in one call
  > Observed by this seat 2026-09-03T02:31Z. Nothing to move.
- **lens:persona-agent-reader** · `d0012` · crystallize:tension ·t1
  T20 (byte-cut continue cost ~71k tokens on one call) has no PLAN row; the reader pays it on every truncation until someone owns it
  > Tension for the captain: keep bytes (§7-correct, cheap server) or record-align continue (parseable slices, a v2 row). Neither is free; the plan is silent.
- **lens:persona-agent-reader** · `d0013` · crystallize:accept ·t2
  401/404/405 each carry the fixing call (observed gate 1); recipe without dry_run → 501 with the plan (v2.4)
  > Nothing to move.
- **lens:persona-agent-reader** · `d0014` · uncertain-encode ·t2
  On a fresh host every dry_run answers no history; the reader learns the price only after paying it
  > Defer: DELTA calls this the condition working. Re-ask at v2.7 whether a swagger-derived or house-wide prior (cartographer p50 by family) is an honest basis for a host with no rows.
- **lens:persona-agent-reader** · `d0015` · crystallize:accept ·t1
  v2.8 says docs() cites why the fourth tool exists; it does not say docs() tells a dcs:read seat how to obtain dcs:write
  > A read-only seat that needs a write hits a tool that is not there. PLAN v2.8 done-means gains: docs() on a dcs:read grant names the re-consent step for dcs:write (the pre-formed call, not prose).
- **lens:persona-agent-reader** · `d0016` · trace ·t2
  A free sha is in reach via catalog commit_sha and map-this-release; read-file-at-pin takes the sha as an arg
  > The moving-ref hint is not a chore when the recipe list supplies the sha. Nothing to move.
- **lens:persona-uw-deployer** · `d0017` · trace ·t2
  DEPLOY now names every binding a fork must create; acceptance 7 (second operator from README alone) is still unobserved
  > PLAN row 4 already carries the gap. Nothing to move; the rehearsal is HUMAN-ONLY(uW ops).
- **lens:persona-uw-deployer** · `d0018` · crystallize:accept ·t1
  v2.8 registers mutate only under dcs:write; DEPLOY step 2 (the one HUMAN-ONLY step) says nothing about scopes, and whether a DCS OAuth app can restrict them is unobserved
  > PLAN v2.8 done-means gains: DEPLOY step 2 recut with the scope the operator grants, after observing on git.door43.org whether the OAuth app registration exposes scope selection at all.
- **lens:persona-uw-deployer** · `d0019` · crystallize:tension ·t1
  SPEC gives the operator confirm_required (a belt) but no way to disable mutate for a whole deployment; a second operator's admin has no off switch
  > Tension for the captain: a Worker var (e.g. MUTATE_ENABLED, default off for forks?) vs. scope-only gating (the OAuth app decides). Either is one line in SPEC §mutate; neither is written.
- **lens:persona-uw-deployer** · `d0020` · crystallize:tension ·t1
  consumer_label stores the DCS login per call; for a uW deployment and for V3 (CON-0001) a username is a user identifier the policy must justify or hash
  > Two lenses, one row. T18 asks which label; this asks whether the login should be stored raw at all. Options: keep (it is the operator's own D1), hash it, or store only when ?consumer= is set. Captain rules; policy recut rides v2.7.
- **lens:persona-uw-deployer** · `d0021` · crystallize:accept ·t2
  refused[] is derived from the live swagger and test-pinned against a fixture; a host on another DCS version derives its own list, which the test does not see
  > Nothing to move in v2.8; the PLAN row gains the clause that docs({rung:map}) shows upstream.swagger.version beside refused[] so an operator can see which list applies.
- **lens:persona-uw-deployer** · `d0022` · uncertain-encode ·t2
  DEPLOY says push to main deploys; it does not say where a second operator tests first, and T9 is still undecided
  > Defer to T9's own ruling (second Worker per canon vs prove-on-prod). DEPLOY inherits whichever lands.
- **lens:persona-btservant-dev** · `d0023` · crystallize:accept ·t2
  Three tools (four with write) is under V3's ten-server tipping point; breadth in parameters keeps the prompt override count at zero
  > Nothing to move.
- **lens:persona-btservant-dev** · `d0024` · uncertain-encode ·t2
  observed_at is the first envelope key; anything inlined into a cached prefix busts on every call
  > Defer to the owner: V3's warehouse likely never inlines envelopes, and key order is SPEC-pinned (§7). If the L1 house-envelope lands (v2.x) the stable-first ordering is the place to raise it; noted there, not here.
- **lens:persona-btservant-dev** · `d0025` · crystallize:accept ·t2
  The V3 caller is the warehouse job, not the bot; it wants pins, 304s, archives, and a hand-off shape it can point at its own mapper — v2.6's {server,url,tool,input} is generic enough
  > Nothing to move; v2.6 already ships the generic shape with cartographer as the first target.
- **lens:persona-btservant-dev** · `d0026` · crystallize:accept ·t2
  Every cooked v2 row records a divergence from SPEC in the row itself (v2.1 compact params); SPEC is recut beside it — CON-0004 satisfied as practised
  > Nothing to move.
- **lens:persona-btservant-dev** · `d0027` · crystallize:accept ·t2
  88 tests, a fresh-seat validation doc, and a my-spend recipe: V3 can evaluate without trusting
  > Nothing to move. CON-0010 (content vs tooling): this server is content access; trace, no row.
