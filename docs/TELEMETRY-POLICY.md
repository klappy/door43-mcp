# Telemetry policy
Tracked: tool name, method, first two path segments, HTTP status, duration, bytes/tokens
out, self-declared consumer label. Not tracked: user identity, full path, query strings,
request or response bodies, tokens. Retention: Analytics Engine default (~3 months).
Same data the maintainer sees is served by the `telemetry` tool — no asymmetry.
