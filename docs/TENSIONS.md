# TENSIONS (open)
- T1 ~~Token-as-bearer unproven.~~ **Resolved 2026-09-03T01:47Z:** `Authorization: token <access>` on `GET /api/v1/user` → 200, login `klappy`, TTL 3600 s, refresh present.
- T2 **`execute` breadth vs. authz parity.** Passthrough trusts DCS to enforce
  permissions. Holds for reads; v2 writes need mirrored confirmations. **Observed 2026-09-03 (gate 1):** the edge allowlists method (GET/HEAD) and path shape (`/api/v1/…` or `/{o}/{r}/archive/{ref}.zip` HEAD-only); `?`, `#`, `..`, `//` in `path` → 400 with no upstream fetch (test asserts); forwarded headers are allow-listed (accept, accept-language, if-none-match, if-modified-since, range). Authz itself stays DCS's — every 4xx passes DCS's body through unchanged. Open for v2 as written.
- T3 **Rulings not in canon.** "CF library, never hand-rolled" and "3–4 tools" were
  chat/journal rulings until klappy.dev#315 / kitchen#67. Until merged, this repo cites PRs.
- T4 **oddkit URI case.** `klappy://docs/templates/prd-template` 404s; file is
  `PRD_TEMPLATE.md`. Resolver or file needs a rename. (Observed 2026-09-02.)
- T5 **Templates not yet cut.** This doc set is freehand against the 2026-09-02 meeting
  list; the baseline templates will be extracted from it (captain ruling, option 2).
- T6 **`fields` is a projection, not semantics — hold the line.** The moment someone asks
  for `fields` to understand USFM or TSV, that is translation-helps-mcp's job. Retract `fields`
  before letting it grow.
- T7 **Client secret transited chat twice** (2026-09-02; again 2026-09-03 to fix the clipped value — the Worker held it without the `gto_` prefix, 40/44 chars, DCS `unauthorized_client`). **Captain ruling 2026-09-03:** rotate once the whole server is built and proven (gate 4), not per gate. Until then the transcript is a copy; dashboard only when it happens.
- T8 ~~Resources/prompts vs. the ceiling.~~ **Resolved by observation 2026-09-03 (gate 2):** the connector UI on the captain's phone shows tool name + description and nothing else (screenshot 2026-09-02 22:29 ET); resources and prompts have no surface there. Gate 2 ships the boarding pass in `docs()` only — no `door43://boarding-pass` resource, no prompts. SPEC §resources recut to say so. If a client surfaces them later, add them then (they stay outside the tool cap).
- T9 **Single-env DO worker vs. `per-environment-worker-projects`.** `McpAgent` is a Durable Object; canon
  says DO-backed workers get separate dev/staging/prod projects. Gate 0 deploys one `door43-mcp`. **Observed 2026-09-03:** CF issues no version preview for a DO-bound Worker (`has_preview:false` on every green build), so branch previews are structurally unavailable here. Decide now, not gate 1: (b) merge-to-main proves on prod, or a second Worker `door43-mcp-dev` per canon.
- T10 **Door without a deploy credential.** GitHub-write + no CF token = a door that can build but not fly
  (same shape as the 2026-08-31 read-only-door finding). Either seats carry a scoped 1h CF token, or deploy rides
  Workers Builds on push. (Observed 2026-09-02.)
- T11 ~~Convention cited as merged, file says proposed.~~ **Closed 2026-09-03.** Header recut in kitchen#69: `Status: law (merged 2026-09-02)`. Seat miss: read a stale zoom, not the head.
- T12 **Telemetry sampled channel needs a credential the seat does not hold.** House prior art (cartographer) serves two channels: exact (D1, no credential) and sampled (Analytics Engine, `SUM(_sample_interval)`, needs a CF read token for the SQL API). Gate 3 ships exact only. Wiring AE means a Worker secret (`CF_AE_READ_TOKEN` or equivalent) — HUMAN-ONLY(secret). Until then `telemetry` answers from D1 alone and says `exact: true` on every answer. (Observed 2026-09-03, gate 3.)
- T13 **Statelessness vs. the session map.** The Durable Object already holds a refreshed grant (`grant:refreshed`), so "stateless" is a claim about *what kind* of state: the DO holds auth and nothing about the session; everything else rides in the client's envelopes or in sealed tokens the server does not remember (`continue`, `confirm`). A session map on the server (seed 1) is rejected on this line; the derived form is a `telemetry` recipe. No `session` column. (Opened 2026-09-03, planning pass; `docs/DELTA.md`.)
- T14 **Recipe run vs. §7 and the ceiling.** One tool call that makes N upstream calls hides cost and coarsens the resume point. Held by: per-step envelopes in `body.steps[]`, summed `cost`, one telemetry row per step, `continue` = the remainder, ≤ 5 steps, 200 KB total, no branching, P0005 as the duration line. Open: whether a recipe may end in a *different tool's* call (hand-off) without that being a fourth tool in disguise — the pass says no, it is a pre-formed call the agent still has to make. (Opened 2026-09-03.)
- T15 **Cross-server shape ownership.** Four house servers, four answer shapes (observed 2026-09-03: cartographer `next` as prose, no `cost`; oddkit `server_time`/`assistant_text`/`debug`; gitauth flat `quota`; door43 §7). `handoff` and `steps[]` have no owner: L1 (klappy.dev pattern) or L4 (kitchen convention §7)? Until ruled they ride in `body`, and the L1 targets are named in `docs/DELTA.md`. Retrofits are sibling tickets, not this repo. (Opened 2026-09-03.)
- T16 **L2 hid the keys the driver needed.** `responseKeys` elides array-item keys past 12 with `…`; on `/catalog/search` that hid `commit_sha` and `zipball_url` (observed 2026-09-03T03:56Z, 5,731-byte answer). The pass selected them from memory — the failure the ladder exists to prevent. Closes when v2.1 plates; recorded here so the pinch is not lost if v2.1 is re-ordered. (Opened 2026-09-03.)
- T17 **Writes need confirmations DCS does not have.** Gitea has no two-phase commit; "mirrored confirmations" (convention §3) must be minted here. The pass first proposed a sealed `confirm` on every write; the challenge surfaced E0005.2 session 4 (a two-step on every write is ritual) and the plan changed to **progressive protection**: reversible writes in one call, a sealed `confirm` bound to `(method, path, body sha256, sub, exp)` only for the destructive-but-undoable class, refusal for the irreversible class. All three classes must be **observed** from the swagger's operations, never guessed — the classification itself is the open design question. Open: whether writes are a v2 gate (v2.8, last) or a separate meal — captain's ruling at taste. (Opened 2026-09-03.)
- T18 **Which label is `consumer_label`?** The grant login is verified by DCS; `?consumer=` is self-declared (oddkit/cartographer ladder). Both cannot be one column without lying about provenance. Proposed: `consumer_label` = query label when present else grant login; `consumer_source` says which. Telemetry policy recut when v2.7 plates. (Opened 2026-09-03.)
