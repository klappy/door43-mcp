# Validation — door43-mcp v1 as built

Validator: Otto, fresh seat for gates 0–3 (this door cooked only the post-gate fixes #8 and #10 — named, not hidden).
Observed against `door43.klappy.dev`, main `acb19aa` (0.3.2), upstream `git.door43.org 1.27.2+dcs`,
window 2026-09-03T04:36:11Z–04:37:33Z, via the seat's own connector (`tools/list` = the three
🚪 Door43 tools in the harness). Mode: `klappy://canon/validation-as-epistemic-mode` — observe, do not fix.

## PRD acceptance 1–11

| # | Item | Verdict | Call | Observed |
|---|---|---|---|---|
| 1 | `tools/list` exactly three | **observed** | harness tool list | `docs`, `execute`, `telemetry`; no fourth |
| 2 | Login round-trip; `execute GET /user` → login | **observed** | `execute GET /user fields[login,id]` 04:36:14Z | 200 `{login:"klappy",id:708}` 27 B, upstream 102 ms |
| 3 | `/catalog/search?limit=1` → an entry | **observed** | `execute GET /catalog/search {limit:1} fields[data[].full_name]` 04:36:17Z | 200 `OBS-TLF/ahr_obs`; `x-total-count 608` |
| 4 | `docs` carries live DCS version | **observed** | `docs()` 04:36:11Z | `upstream.version "1.27.2+dcs"` on every envelope |
| 5 | `execute POST` refused naming v2 | **observed** | `execute POST /user` 04:36:22Z | 405 `"POST is not in v1"`; hint names v2; `upstream_ms 0` (no fetch) |
| 6 | `telemetry` SELECT works; non-SELECT refused | **observed** | `SELECT tool_name, COUNT(*)…` 04:36:28Z · `DELETE FROM …` 04:36:30Z | 200 rows execute 11 / docs 6 / telemetry 4, `exact:true` · 400 `"SELECT only"`, hint "nothing was run" |
| 7 | Second operator deploys from README alone | **not observed** | — | Needs uW ops on `qa.door43.org`. DEPLOY recut in this PR removes the known blockers (F5); the rehearsal itself is the named gap on PLAN row 4 |
| 8 | `docs()` < 2 KB, names the user | **observed** | `docs()` 04:36:11Z | 1215 B; `auth.logged_in_as "klappy"` |
| 9 | `/catalog/search?limit=1` + `fields` < 300 B | **observed** | same as #3 | 42 B — but selector is `data[].full_name`, not PRD's `[].full_name` (F3) |
| 10 | `continue` replayed verbatim → next slice | **observed** | `execute GET /catalog/search {limit:100,showIngredients:true} fields[data[].full_name,data[].ingredients]` 04:36:56Z → `continue:"eyJvZmZzZXQiOjIwNDgwMH0"` 04:37:08Z | slice 1: `truncated:true`, 204800 B of 283868; slice 2: starts at byte 204800, 79068 B, "last slice". 204800+79068 = 283868 ✓ |
| 11 | `/api/v1/nonexistent` → 404 + three nearest paths | **observed** | `execute GET /api/v1/nonexistent` 04:36:26Z | 404; hint lists `/notifications`, `/notifications/new`, `/notifications/threads/{id}` |

Also observed: journey 2 end to end (`docs({recipe:"latest-release-zip"})` 273 B → `execute GET /repos/unfoldingWord/en_ult/releases/latest` → `v90`, `zipball_url` present, 145 B) · `docs({path:"/catalog/search"})` 5731 B · root `/` 200 prints `door43-mcp 0.3.2` (= `package.json`, HYGIENE 19) · `/health` 200 upstream 358 ms.

## Findings

| # | Finding | Disposition | Where |
|---|---|---|---|
| F1 | **DELTA pinch 1 confirmed.** `docs({path})` `response_keys` cut at 12 with `…`; `zipball_url`/`commit_sha` hidden. | iterate | v2.1 (already queued) |
| F2 | **DELTA pinch 2 confirmed.** `next.query` retypes numbers as strings (`limit:"1"`, `page:"2"`, `showIngredients:"true"`); `continue.query` keeps the input's types. Two pre-formed calls, two type policies. | iterate | v2.2 (already queued) |
| F3 | PRD acceptance 9 spells the selector `[].full_name`; the body is `{data:[…]}` so only `data[].full_name` works. PRD text is stale, not the server. | accept | TENSIONS T20 |
| F4 | `continue` slices are byte cuts of the serialized body; slice 2 begins mid-token and is not JSON on its own. Hint says so; a consumer must concatenate all slices before parsing. Validating item 10 cost ~71k tokens for one call. | accept (by design, §7); name the cost | TENSIONS T20 |
| F5 | DEPLOY named `wrangler.toml` (repo has `wrangler.jsonc`) and omitted three things an operator must change or create: `kv_namespaces[0].id`, `d1_databases[0].database_id`, `routes[0].pattern`. A fork with these values deploys onto the wrong bindings. | fix here (docs) | DEPLOY recut in this PR |
| F6 | README status line said "planning — no code yet" against a live 0.3.2. | fix here (docs) | README recut in this PR |
| F7 | Telemetry `count` column exists beside `COUNT(*)`; the boarding-pass journey uses `COUNT(*)`. Harmless; naming so nobody "fixes" it into the AE `SUM(_sample_interval)` shape on the exact channel. | accept | none |

No `src/` change in this PR (ticket failure mode 2).
