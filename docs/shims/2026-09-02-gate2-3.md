# SHIM — door43-mcp gates 2+3 (Otto seat, `docs` + `telemetry`)

```
You are Otto, infra seat, klappy stack. Board from
https://github.com/klappy/door43-mcp/blob/main/docs/shims/2026-09-02-gate2-3.md
then cook klappy/kitchen rail/1-ordered/2026-09-02-door43-mcp-gate2-3-docs-telemetry/TICKET.md.
Report on the rail, not in chat.
```

## Board (fetch live, in this order)
1. `oddkit_time`. 2. `klappy://canon/bootstrap/model-operating-contract`.
3. GitAuth → `door43-mcp` `contents:write,pull_requests:write` (+ `workflows:write`
   only if you touch `.github/`). `/home/claude/.tok`, umask 077, `rm -f` after.
4. This repo main: `AGENTS.md` → `docs/PLAN.md` (rows 0, 1a, 1b proven; 2, 3
   yours) → `docs/SPEC.md` §docs §telemetry → `docs/TENSIONS.md` T8.
5. The ticket, whole, then its CHECKLIST-RUN.
6. Law: convention §2 §8 §10 · `klappy://canon/constraints/mcp-tool-surface-ceiling`
   · `klappy://canon/constraints/infra-config-is-seat-work`.
7. Prior art, read before writing: cartographer `docs` + `telemetry` (call them
   via MCP if the seat has them; else read `klappy/cartographer` `src/`).
8. `oddkit_preflight` on "docs ladder + telemetry for door43-mcp".

## Already true (verify)
- `execute` live on `door43.klappy.dev` (main `ff3cd71`), proven 02:31Z.
- D1 `door43mcp_telemetry` `52fa8ea4-7774-4135-b160-f17bb41ac660` exists. Bind
  it; do not create another.
- Deploy is push; branch = `versions upload`, no preview (T9); prove on main
  after merge, as gates 0 and 1 did.

## Not this plate
AE sampled channel (needs a credential — T12), writes, uW deploy, T7 rotation.

## Report
Draft PR with: `docs()` output verbatim (≤2 KB), one `docs({path})`, one
`docs({recipe})`, a `telemetry` SELECT before/after one `execute`, the rejected
`DROP`, build UUID, `0.3.0`. Rows 2/3 filled. Draft until the captain reads.
