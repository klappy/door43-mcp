# door43-mcp — Plan

| Gate | Work | Done-means | Owner |
|---|---|---|---|
| 0 | **Spike**: register OAuth app (HUMAN-ONLY), minimal worker, login, `GET /api/v1/user` | 200 with login name, screenshot in PR | Otto |
| 0 · **observed 2026-09-03T01:47:36Z** | **Gate 0 open→proven.** On `door43.klappy.dev` (main `1ba92e9`, build `6935df60`, deploy by push): DCS consent (`openid profile email`) → `/callback` → token exchange OK → `GET /api/v1/user` with **`Authorization: token <access>` → 200** `application/json`, **login `klappy`**, `expires_in 3600s`, refresh token present, `token_type bearer`, upstream 313 ms. T1 resolved: DCS OAuth tokens authorize `/api/v1`. Blockers on the way: `D43_CLIENT_SECRET` stored without its `gto_` prefix (40 vs 44 chars, `unauthorized_client`) — reset by API 01:36Z; userinfo path returned HTML, replaced by the `/api/v1/user` observation. Not observed: `whoami` via `/mcp` (client code not exchanged yet) — same call, same header, behind the provider. Prod deploy history: 01:12Z API upload (bootstrap exception, stub), then every deploy by push. | 200 + login `klappy` observed; screenshot in PR | Otto |
| 0 · **closed 2026-09-03T03:49Z** | debug page retired (#8): `/callback` success → 302 to the client; proof page only behind `?debug=1` sealed at `/authorize`, login only. `test/auth.test.ts` (5 pass). | 302 observed in test; captain reconnects with zero extra taps | Otto |
| 1a · **cooked 2026-09-03T02:12Z, branch `gate1-execute`** | `execute` GET/HEAD + envelope + refresh-on-401 + teaching errors. Observed in `test/execute.test.ts` (21 pass, vitest 3.2.7, injected fetch): ten envelope keys pinned in SPEC order on 200 and 405; `GET /user` → `body.login` from upstream with `Authorization: token`; POST → 405 + v2 hint, **0 upstream fetches**; path write-leaks (`?action=`, `..`, `#`, `//`) → 400, 0 fetches; 401 → one refresh, one retry, `hints[]` "refreshed", second 401 → envelope 401 + re-login hint, exactly 2 fetches; 404 → three swagger paths in one hint; 403 body passed through. Durable refresh rides the provider's `tokenExchangeCallback` (`src/index.ts`); an in-`execute` refresh is kept in DO storage for the session. `whoami` retired. **Not yet observed on `door43.klappy.dev`** — prove on main after merge (deploy is push, T9: no branch preview). Commit: see PR. | Acceptance 2, 3, 5, 11 | Otto |
| 1b · **cooked 2026-09-03T02:12Z, same branch** | `fields` projection (`src/projection.ts`: selection only, unknown → `null`, no renames/defaults — T6), 200 KB cap after projection (`src/cap.ts`), `continue` as a pre-formed execute call carrying an opaque byte-offset token, `next` from `Link: rel="next"` (+ `x-total-count` hint). Observed in tests: `["data[].name","data[].owner.login"]` → body with only those keys, byte-identical on a second call; 20 000-item body → `truncated:true`, `cost.bytes` = 204 800, `continue` round-trips until the concatenated slices parse to the projected body; small body → `truncated:false`, `continue:null`. Commit: see PR. | Acceptance 9, 10 | Otto |
| 2 · **cooked 2026-09-03T02:5xZ, branch `gate2-3-docs-telemetry`** | `docs({rung?, path?, query?, recipe?})` (`src/tools/docs.ts`). Observed in `test/docs-telemetry.test.ts` against a slice of the live swagger (`test/fixtures/swagger-slice.json`, 11 of 323 paths, observed 02:50Z) and once against the full 323-path document: L0 pass **1215 bytes body / 1647 bytes whole envelope** (cap 2048, test fails at 2049), cites both constraint URIs, `server.version 0.3.0`, `upstream.version 1.27.2+dcs`, `auth.logged_in_as`; **zero swagger/DCS calls for the pass** (test asserts); L1 map = 6 families with counts (catalog 11 · repos 173 · user 35 · users 16 · orgs 27 · misc 61 = 323) + 3 paths each; L2 `/catalog/search` = params, `response_keys`, quirks incl. "owner is a string" (swagger `CatalogEntry.owner: string` agrees); L3 = the swagger fragment verbatim; `query` = BM25 over path+summary+tags ("latest release" → `/repos/{owner}/{repo}/releases/latest` first); 5 recipes shipped, `latest-release-zip` selects `zipball_url`. Resources/prompts **not** shipped (T8 resolved by observation). **Not yet observed on `door43.klappy.dev`** — prove on main after merge (deploy is push). | Acceptance 4, 8 | Otto |
| 3 · **cooked 2026-09-03T02:5xZ, same branch** | `telemetry({sql})` (`src/tools/telemetry.ts`) on the **exact channel**: D1 `door43mcp_telemetry` `52fa8ea4-7774-4135-b160-f17bb41ac660` (created by CoS 02:32Z, observed by API 02:50Z: `num_tables 0`, ENAM) bound as `TELEMETRY_DB` in `wrangler.jsonc`; schema in `src/telemetry/schema.sql` applied by `ensureSchema` (`CREATE … IF NOT EXISTS`, once per isolate) — mirrored in `schema.ts`, test pins file == constant. Parser gate `isReadOnlySql`: single statement, `SELECT`/`WITH` only, no `;`, denylist of mutating/admin words; test drives INSERT · DROP · `;` · PRAGMA · ATTACH · UPDATE · DELETE → 400 envelope, **0 statements reach D1**. Writer: every tool call → one row via `ctx.waitUntil` (`Door43MCP.emit`), column allowlist `TELEMETRY_COLUMNS` (oddkit set + `upstream_status`, `upstream_ms`, `path_family`); test asserts an execute on `/repos/unfoldingWord/en_ult/contents/README.md?ref=v86` yields one row with `path_family:/repos` and none of `unfoldingWord`/`README`/`v86`/query in it. AE sampled channel **not** cooked (T12). **Not yet observed on `door43.klappy.dev`** — the D1 table is created by the first request after merge. | Acceptance 6 | Otto |
| 4 | README/DEPLOY, second-deploy rehearsal on `qa.door43.org` | Acceptance 7 | Otto + uW |
| 5 | Directory-listing gauntlet (optional, public) | Listing submitted | captain |

Observed 2026-09-02: OAuth app registered (client id `4d2afcd1-…`), Worker `door43-mcp`
exists with three secrets + `D43_HOST`; secret to be rotated after gate 0 (transcript exposure).
HUMAN-ONLY: gate 5 listing account; secret rotation after the full build is proven (captain ruling 2026-09-03; `D43_CLIENT_SECRET` transited chat twice).
Challenge (`oddkit_challenge`, planning) re-run after gate 0 with the spike result.
Validation: external validator reads SPEC boundaries against `tools/list` and one
transcript of journey 2.

## v2 (draft — `2026-09-02-door43-mcp-v2-planning`; ordered by ergonomics-per-cost; nothing cooked)

Contracts: `docs/SPEC.md` §v2. Receipt: `docs/DELTA.md`. Done-means are agent-observable (P0007). Owner column is a proposal; the captain names seats at taste.

| Gate | Work | Done-means (observable) | Owner |
|---|---|---|---|
| v2.1 | **L2 tells the whole truth, cheaply.** `response_keys` complete; `detail:"compact"` default, `"full"` on ask; `fields` on `docs`. | `docs({path:"/catalog/search"})` ≤ 2 KB and its `response_keys` contains `zipball_url` and `commit_sha`; `detail:"full"` returns the param prose; `docs({path, fields:["response_keys"]})` returns only that key; L2 on the live swagger for every path with a 200 schema lists every key (test walks all 323). | Otto |
| v2.2 | **Teach on 200; typed `next`; conditional reads; rate-limit tally.** | Replay of a `next` from a call made with `limit:3` sends `limit=3` and the echoed `next.query.limit` is a number; `execute` with `headers:{"if-none-match":<etag from prior upstream.etag>}` → `status 304`, `body null`, `cost.bytes 0`, hint `unchanged`; `fields:["data[].owner.login"]` on `/catalog/search` → hint naming the schema (`owner` is a string); `upstream.ratelimit` present when DCS sent it (observe first; if DCS sends none, the row records that and the field stays null); every hint on 200 → 0 extra fetches (test asserts). | Otto |
| v2.3 | **Pins.** `upstream.swagger{version,etag,observed_at}` on every docs L1–L3; `pin:{sha}` on `/repos/*` and archive; catalog/refs answers carry the sha the upstream gave. | `execute GET /repos/{o}/{r}/contents/README.md {ref:"master"} pin:{sha}` → echoed `request.query.ref` equals the sha and the upstream URL had `?ref=<sha>` (test); `docs({rung:"map"})` carries `upstream.swagger.etag`; no test case spends an extra fetch to obtain a sha. | Otto |
| v2.4 | **Recipe grammar: `args`, templates, `dry_run`.** | `docs({recipe:"latest-release-zip", args:{owner:"unfoldingWord",repo:"en_ust"}})` → calls with that path; missing `owner` → 400 naming `owner` with its `about`; `execute({recipe, args, dry_run:true})` → `body.plan` + `body.estimate{bytes,calls,basis}` and **0 upstream fetches** (test); `basis` is a string naming the telemetry query or `"no history"`. | Otto |
| v2.5 | **Recipe run.** ≤ 5 straight-line steps, per-step envelopes, summed cost, `continue` on failure. | 3-step recipe → `body.steps.length 3`, `cost.bytes` = Σ steps; step 2 forced to 404 (injected fetch) → `steps.length 2`, `truncated:true`, `continue` = `{recipe,args,from:2}` and replaying it runs steps 2–3 only; `telemetry` shows 3 `tool_call` rows + 1 `recipe_run` row for the run; a recipe declared with 6 steps fails the test suite (bound is enforced at definition). | Otto |
| v2.6 | **Hand-off.** `map-this-release` ends in `body.handoff` = a pre-formed cartographer call. | `execute({recipe:"map-this-release", args:{owner,repo}})` → `body.handoff = {server:"cartographer", url:"https://cartographer.klappy.dev/mcp", tool:"execute", input:{capability:"consult_repo", payload:{zip_url}}, provenance:{sha, ref, observed_at}}`; pasting `input` into cartographer returns a map whose `provenance` is `unpinned` and whose sha matches `handoff.provenance.sha` (recorded in the PR as observed, not assumed); one round-trip through a fresh seat with no memory of this repo. | Otto, Cartographer owner consulted |
| v2.7 | **Consumer ladder + `my-spend`.** `?consumer=` → `consumer_source: query`; `my-spend` recipe over `telemetry`. | Connect with `/mcp?consumer=probe` → the row for the first call has `consumer_label probe, consumer_source query`; without it, `grant`; `docs({recipe:"my-spend", args:{since:"2026-09-03T00:00:00Z"}})` returns a `telemetry` call that, executed, answers bytes and calls for that label. | Otto |
| v2.8 | **Writes, progressive protection (phase 2b — T17).** | `execute POST /repos/{o}/{r}/issues body:{…}` → one upstream POST and a 201 envelope (reversible class, no seal); `DELETE /repos/{o}/{r}/branches/x` → `428` + `body.confirm` pre-formed; replay with `confirm` → exactly one upstream DELETE (test asserts one fetch); replay after 10 min → 428 again; tampered seal → 400; `DELETE /repos/{o}/{r}` → 403 with the HUMAN-ONLY hint and 0 fetches; `docs({rung:"map"})` lists `confirm[]` and `refused[]` derived from the swagger's operations (test pins the derivation against the fixture). `tools/list` still 3. | Otto; captain rules whether this is a separate meal |
| v2.x | **House shape (L1).** Not this repo — `docs/DELTA.md` §Proposed L1 items. | Each target URI resolves via `oddkit_get`, and cartographer/oddkit/gitauth each carry a retrofit ticket on the rail. | CoS |

Challenge (`oddkit_challenge`, planning) run after the pass; result in `docs/DELTA.md`.
Validation: a fresh seat reads SPEC §v2 against this table and cuts the v2.1 ticket without a question (ticket done-means 2).
